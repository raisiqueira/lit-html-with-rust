# simple web component with Rust

Simple counter component written in Rust + Lit. Just for fun!

## Running

Run the build command to generate the wasm file:

```bash
make build
```

And run the `index.html` file.